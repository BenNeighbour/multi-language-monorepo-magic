syntax = "proto3";

message Person {
  string name = 1;
  int32 id = 2;
  string email = 3;
}

message Empty {}

message AddressBook {
  repeated Person people = 1;
}

service Hello {
  rpc SayHello(Empty) returns (Person);
}